name: coleapp
description: "Colecheck - Gestión educativa móvil"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  get_it: ^9.1.1
  shared_preferences: ^2.5.3
  http: ^1.3.0
  injectable: ^3.0.0
  logging: ^1.3.0
  tutorial_coach_mark: ^1.3.3
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  injectable_generator: ^3.1.0
  build_runner: ^2.15.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
