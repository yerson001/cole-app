import 'package:flutter/material.dart';

class FotocheckContent extends StatelessWidget {
  const FotocheckContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fotocheck'),
    );
  }
}
